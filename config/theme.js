const colors = {
  body: "#FFFFFF",
  link: "#8B008B",
  mainText: "#FFFFEE",
  divisors: "#FFAAEE",
}

const images = {
  width: "64px",
}

const theme = {
  colors,
  images,
}

export default theme
