const colors = {
  body: "#eaeaea",
  mainText: "#444444",
}

const divisors = {
  color: "#f30067",
  borderColor: "#00d1cd",
  border: "15px solid #00d1cd",
  text: "#eaeaea",
  headerHeight: "120px",
  footerHeight: "90px",
}

const images = {
  width: "64px",
}

const theme = {
  colors,
  images,
  divisors,
}

export default theme
